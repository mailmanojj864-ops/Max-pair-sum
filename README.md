# Max Pair Sum in C

A simple C program to find the **sum of the two largest numbers** in an array. The program handles multiple test cases and works efficiently for arrays of any size.

## Features
- Finds the largest and second largest elements in the array.
- Computes their sum.
- Supports multiple test cases.
- Handles negative and positive integers.

